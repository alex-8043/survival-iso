{
  "name": "survival-iso",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "Survival isométrico 2D (pixel-art). Simulación ECS headless en Web Worker + render PixiJS v8. Tanda H: cámara suave, zoom/paneo, biomas grandes, aldeas, oro/diamante, camas.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "bitecs": "^0.3.40",
    "pixi.js": "^8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
